PROJECT (accessl-common)

SET(COMMON_SOURCE log.c stat.c)

SET(CMAKE_C_FLAGS "-fPIC ${CMAKE_C_FLAGS}")
SET(LINK_FLAGS "-fPIC ${LINK_FLAGS}")

FIND_PACKAGE(Log4C REQUIRED)

ADD_LIBRARY(accessl-common STATIC ${COMMON_SOURCE})
